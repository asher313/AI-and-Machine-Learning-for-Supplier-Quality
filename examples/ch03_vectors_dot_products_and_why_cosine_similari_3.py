# Chapter 3 — 3.1 Vectors, Dot Products, and Why Cosine Similarity Is a Dot Product
apex = np.array([12.0, 1.8, 0.981, 0.96])

print(round(cosine(cobalt, redline), 4))
print(round(cosine(cobalt, apex), 4))
