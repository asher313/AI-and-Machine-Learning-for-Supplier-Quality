# Chapter 4 — commands

## Asher at Northlake

ttings()` provides the connection string; SQLAlchemy's `create_engine` opens it; `text()` wraps raw SQL. The file contains two statements, so the function splits on the semicolon and runs each. These are the first new dependencies since Chapter 2 — SQLAlchemy and the PostgreSQL driver its URL names:

```bash
uv add sqlalchemy "psycopg[binary]"
```

Running it is the same on every operating system, because `uv` handles the environment:

```bash
uv run python -m sqm_ai.features
```
