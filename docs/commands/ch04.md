# Chapter 4 — commands

## Asher at Northlake

ttings()` provides the connection string; SQLAlchemy's `create_engine` opens it; `text()` wraps raw SQL. The file creates the table if needed, then truncates and refills its rows in one transaction. Keeping the table object preserves dependent views, and a failed insert rolls back the refresh. This small example splits the controlled SQL file on semicolons and runs each statement with bound date parameters. That is safe only for this controlled file with no semicolons in strings, procedural blocks, or comments; use a migration tool or explicit statement list for general SQL. These are the database dependencies for this chapter — SQLAlchemy and the PostgreSQL driver its URL names:

```bash
uv add sqlalchemy "psycopg[binary]"
```

Running it is the same on every operating system, because `uv` handles the environment:

```bash
uv run python -m sqm_ai.features
```
