# Chapter 17 — commands

## 17.2 The Minimal Pipeline

This chapter adds four dependencies; install them once before the first listing (the same `uv add` habit as Chapter 2):

```bash
uv add sentence-transformers "psycopg[binary]" pgvector rank-bm25
```
