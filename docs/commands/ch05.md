# Chapter 5 — commands

## 5.4 Python to HANA

never by formatting them into the SQL string; `hdbcli` uses `?` as its placeholder. String-formatted SQL is how injection happens, and even with a trustworthy value, a parameterized statement lets HANA reuse its compiled plan instead of compiling a new one per date. SAP's driver is a new dependency:

```bash
uv add hdbcli
```

## Asher at Northlake

```bash
uv run python -m sqm_ai.extract
```
